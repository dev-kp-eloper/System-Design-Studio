apiVersion: apps/v1
kind: Deployment
metadata:
  name: sysdesign-redis
  labels:
    app: sysdesign-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sysdesign-redis
  template:
    metadata:
      labels:
        app: sysdesign-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: sysdesign-redis
spec:
  ports:
    - port: 6379
  selector:
    app: sysdesign-redis
