apiVersion: apps/v1
kind: Deployment
metadata:
  name: sysdesign-postgres
  labels:
    app: sysdesign-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sysdesign-postgres
  template:
    metadata:
      labels:
        app: sysdesign-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: sysdesign
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: sysdesignpass
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: sysdesign-postgres
spec:
  ports:
    - port: 5432
  selector:
    app: sysdesign-postgres
